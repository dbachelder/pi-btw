# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | scope | BTW interaction model | Replace the current one-turn widget flow with a lightweight modal multi-turn side chat | The user wants a real inline chat interaction without turning BTW into a full second workspace | No |
| D002 | M001 | convention | BTW behavioral contract | Preserve the current README-documented semantics for `/btw`, `/btw:new`, `/btw:clear`, and `/btw:tangent` | The user explicitly wants the interaction model changed without changing BTW thread semantics | No |
| D003 | M001 | pattern | Main-session boundary | Keep BTW handoff to the main session explicit via inject/summarize-style actions | The user wants BTW to remain a tangent and not auto-pollute the main session | No |
| D004 | M001 | scope | Slash-command ambition | Treat broad slash-command support as desirable but feasibility-sensitive | The user wants to see what is possible, but not at the cost of making BTW heavy or messy | Yes — if pi exposes a clean embedded command surface |
| D005 | M001/S01 | pattern | BTW modal architecture | Keep hidden BTW entries and reset markers as the source of truth and layer the modal over them via `ctx.ui.custom(..., { overlay: true })` | This preserves existing thread semantics for later slices while replacing the interaction surface | No |
| D006 | M001/S01 | verification | Pi TUI fallback proof | Use a narrow Vitest runtime harness for overlay/thread/error semantics when full live TUI automation is blocked by environment issues | The modal contract still needs executable proof even when terminal automation is unavailable | Yes — live TUI proof should supersede it when available |
| D007 | M001/S02 | verification | BTW contract gate | Treat `tests/btw.runtime.test.ts` as the executable gate for BTW command, restore, and context-boundary semantics, and only change production helpers when a named contract assertion fails | This keeps hidden entries and reset markers authoritative and avoids speculative churn in `extensions/btw.ts` | Yes — if broader runtime coverage later proves this gate insufficient |
| D008 | M001/S03 | verification | Overlay dismissal contract | Treat overlay dismissal during handoff and Escape as a production contract proven through runtime assertions, not a purely visual assumption | Successful inject/summarize and dismiss flows depend on a live overlay close path; visual rendering alone would miss lifecycle regressions | Yes — live runtime proof could later supplement it |
| D009 | M001/S03 | pattern | Overlay runtime reuse | Preserve and reuse the existing overlay runtime across dismiss/reopen cycles instead of forcing a new overlay handle on every reopen | The tracked runtime is the truthful lifecycle owner; premature cleanup breaks handoff and dismissal semantics | Yes — if future UI constraints require a different lifecycle model |
| D010 | M001/S04 | scope | BTW modal slash policy | Support only BTW-owned slash commands inside the modal and treat broader slash parity as deferred unless pi later exposes a clean embedded command surface | This satisfies useful in-modal command support without turning BTW into a second heavy command line or duplicating unproven main-input behavior | Yes — if pi later offers a clean reusable embedded command dispatcher |
| D011 | M002 | arch | BTW session model | Replace manual streamSimple/completeSimple plumbing with a real AgentSession sub-session via createAgentSession() with SessionManager.inMemory() | A real session gives BTW tools, slash commands, compaction, and proper message lifecycle for free instead of hand-rolling each capability (supersedes D010 for slash scope) | No |
| D012 | M002 | arch | Sub-session extension policy | Skip bindExtensions() on the BTW sub-session — no extension runner, no recursive extension loading | Extensions belong to the main session; the sub-session needs only tools and the agent loop. All extension runner accesses in AgentSession are null-guarded. | Yes — if specific extensions need to run in BTW |
| D013 | M002 | arch | BTW concurrency model | BTW sub-session runs parallel to the main session with no write coordination | User accepted concurrent cwd access as their responsibility. Both sessions are independent state machines. | Yes — if concurrent access causes real user confusion |
| D014 | M002 | arch | BTW persistence model | Ephemeral sub-session via SessionManager.inMemory() — no disk persistence, no cross-restart restore | Keeps BTW lightweight and disposable. Supersedes M001's hidden-entry persistence model for the active thread. | Yes — if users need persistent BTW sessions |
| D015 | M002/S01 | pattern | BTW sub-session bootstrap | Create BTW sub-sessions with a zero-extension ResourceLoader seeded from the main session prompt plus BTW append prompt, then preload contextual/tangent history via `session.agent.replaceMessages(...)` | This keeps BTW fast, avoids recursive extension loading, preserves main-session auth/model state, and makes contextual seeding explicit and testable through the AgentSession event loop | Yes — if pi later exposes a first-class session seeding API or extension-safe BTW runtime |
| D016 | M002/S01 | pattern | BTW sub-session disposal ownership | Make the BTW session runtime own its event unsubscriptions and await `abort()`/`dispose()` before thread resets or session replacement | Escape, `/btw:clear`, and `/btw:new` all need the same leak-free cleanup path; centralizing disposal keeps listener teardown explicit and prevents overlapping sub-session lifecycles | Yes — if pi later guarantees listener cleanup and replacement ordering inside AgentSession |
| D017 | M002/S02 | observability | BTW transcript inspection surface | Treat `BtwTranscriptEntry[]` as the canonical BTW runtime model and expose it through `BtwOverlayComponent.getTranscriptEntries()` for tests | The mapper and renderer now share one typed event-driven state, and tests can inspect streaming/failure rows without scraping rendered text only | Yes — if a dedicated transcript store or external inspector replaces overlay-local access |
| D018 | M002/S02 | pattern | BTW overlay render hierarchy | Render tool results as indented child output beneath tool-call rows instead of separate flat badge blocks | This keeps tool activity visually grouped while preserving the existing line-based viewport and scroll pipeline for multi-line transcript content | Yes — if later UX work introduces collapsible tool sections or structured transcript widgets |
| D019 | M002/S02 | lifecycle | BTW overlay event subscription ownership | Make the overlay/session lifecycle own the BTW sub-session event subscription and discard hidden-session events by unsubscribing on close/dispose instead of buffering them | T03 requires leak-free listener teardown and real-time overlay refresh while open; unsubscribing on dismiss prevents late disposed-session events from mutating transcript state | Yes — if future UX explicitly needs hidden-overlay event buffering |
